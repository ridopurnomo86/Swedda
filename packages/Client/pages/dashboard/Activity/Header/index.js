import React from "react";
import { HeaderContainer, TitleText, BookIcon } from "./styles";

const Header = () => (
    <HeaderContainer>
        <BookIcon />
        <TitleText>Learning Activity</TitleText>
    </HeaderContainer>
);

export default Header;
