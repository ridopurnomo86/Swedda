import styled from "styled-components";

const MenuContainer = styled.div`
    display: flex;
    align-items: center;
    gap: 24px;
    .ml-m {
        margin-left: 24px;
    }
    .link {
        cursor: pointer;
        font-weight: 600;
    }
`;

export default MenuContainer;
