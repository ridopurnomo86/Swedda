import styled from "styled-components";

const MenuContainer = styled.div`
    display: flex;
    align-items: center;

    .link {
        cursor: pointer;
        font-weight: 600;
        margin-right: 24px;
    }
`;

export default MenuContainer;
