import styled from "styled-components";
import { MainContainer } from "../../../styles";

const EventStaticContainer = styled(MainContainer)`
    min-height: 100vh;
`;

export default EventStaticContainer;
