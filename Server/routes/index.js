const account = require("./account");
const catalog = require("./catalog");
const profile = require("./profile");
const event = require("./event");

module.exports = [account, catalog, profile, event];
